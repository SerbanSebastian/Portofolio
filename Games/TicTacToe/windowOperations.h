#ifndef WINDOW_OPERATIONS_H
#define WINDOW_OPERATIONS_H
#endif
#include "Constants.h"

/*This function creates a simple window that is Full Screen*/
GLFWwindow* initWindow();

/*This function destroies the window that is sent via the parameter*/
void destroyWindow(GLFWwindow* window);

/*This function sets the colour of the background*/

void setColour();